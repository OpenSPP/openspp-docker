# See https://github.com/Tecnativa/doodba#optodoocustomsrcreposyaml
./odoo:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    ocb: https://github.com/OCA/OCB.git
    odoo: https://github.com/odoo/odoo.git
    openupgrade: https://github.com/OCA/OpenUpgrade.git
  target: odoo $ODOO_VERSION
  merges:
    - odoo $ODOO_VERSION
#    - odoo refs/pull/80961/head
# Exaqmple of a merge of the PR with the number <PR>
# - oca refs/pull/<PR>/head
openg2p_registry:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openg2p: https://github.com/OpenG2P/openg2p-registry.git
  target: openg2p 15.0
  merges:
    - openg2p 15.0
#    - openg2p rest-api
# Example of a merge of the PR with the number <PR>
# - oca refs/pull/<PR>/head
openg2p_program:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openg2p: https://github.com/OpenG2P/openg2p-program.git
  target: openg2p 15.0
  merges:
    - openg2p 15.0
#    - openg2p improve-id-dedup-query
# Example of a merge of the PR with the number <PR>
# - oca refs/pull/<PR>/head
openspp_registry:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openspp: https://github.com/openspp/openspp-registry.git
  target: openspp 15.0
  merges:
    - openspp 15.0
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
openspp_program:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openspp: https://github.com/openspp/openspp-program.git
  target: openspp 15.0
  merges:
    - openspp 15.0
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
openspp_communication:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openspp: https://github.com/openspp/openspp-communication.git
  target: openspp 15.0
  merges:
    - openspp 15.0
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
openspp_theme:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openspp: https://github.com/openspp/openspp-theme.git
  target: openspp 15.0
  merges:
    - openspp 15.0
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
openspp_grievance_redress_mechanism:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openspp: https://github.com/openspp/openspp-grievance-redress-mechanism.git
  target: openspp 15.0
  merges:
    - openspp 15.0
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
openspp_demo:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openspp: https://github.com/openspp/openspp-demo.git
  target: openspp 15.0
  merges:
    - openspp 15.0
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
server-auth:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    oca: https://github.com/OCA/server-auth.git
  target: oca 15.0
  merges:
    - oca 15.0
    - oca refs/pull/393/head
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
rest-framework:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    oca: https://github.com/OCA/rest-framework.git
    newlogic: https://github.com/newlogic/oca-rest-framework.git
  target: oca 15.0
  merges:
    - oca 15.0
    - newlogic fix-schema-alias
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
smile-odoo:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    smile: https://github.com/Smile-SA/odoo_addons.git
  target: smile 15.0
  merges:
    - smile 15.0
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
openspp_odoo_external_dependencies:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openspp: https://github.com/openspp/openspp-odoo-external-dependencies.git
  target: openspp 15.0
  merges:
    - openspp 15.0
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
queue:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    oca: https://github.com/OCA/queue.git
  target: oca 15.0
  merges:
    - oca 15.0
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
