# See https://github.com/Tecnativa/doodba#optodoocustomsrcreposyaml
./odoo:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    ocb: https://github.com/OCA/OCB.git
    odoo: https://github.com/odoo/odoo.git
    openupgrade: https://github.com/OCA/OpenUpgrade.git
  target: odoo $ODOO_VERSION
  merges:
    - odoo $ODOO_VERSION
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
registry:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openg2p: https://github.com/OpenG2P/openg2p-registry.git
  target: openg2p main
  merges:
    - openg2p main
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
openspp_registry:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openspp: https://github.com/openspp-project/openspp-registry.git
  target: openspp main
  merges:
    - openspp main
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
openspp_program:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openspp: https://github.com/openspp-project/openspp-program.git
  target: openspp main
  merges:
    - openspp main
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
openspp_communication:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openspp: https://github.com/openspp-project/openspp-demo.git
  target: openspp main
  merges:
    - openspp main
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
#openspp_theme:
#  defaults:
#    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
#    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
#    # for a sane value of 100 commits)
#    depth: $DEPTH_DEFAULT
#  remotes:
#    openspp: https://github.com/openspp-project/openspp-demo.git
#  target: openspp main
#  merges:
#    - openspp main
#    # Example of a merge of the PR with the number <PR>
#    # - oca refs/pull/<PR>/head
#openspp_grievance_redress_mechanism:
#  defaults:
#    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
#    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
#    # for a sane value of 100 commits)
#    depth: $DEPTH_DEFAULT
#  remotes:
#    openspp: https://github.com/openspp-project/openspp-demo.git
#  target: openspp main
#  merges:
#    - openspp main
#    # Example of a merge of the PR with the number <PR>
#    # - oca refs/pull/<PR>/head
openspp_demo:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    openspp: https://github.com/openspp-project/openspp-demo.git
  target: openspp main
  merges:
    - openspp main
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
